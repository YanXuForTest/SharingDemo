name: Hello_pull_request

on:
  pull_request:
  pull_request_target:
  
jobs:
  HelloPRJob:
    runs-on: ubuntu-latest
    steps:
    - name: Say Hello
      shell: pwsh
      run: | 
        Write-Host "hello!"
